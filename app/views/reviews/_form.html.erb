<%= form_for([@restaurant, @review]) do |f| %>
  <%= f.hidden_field :restaurant_id, value: @restaurant.id.to_i  %>
  <%= f.label :content %>
  <%= f.text_field :content %>

  <%= f.label :rating %>
  <%= f.number_field :rating %>

  <%= f.submit %>
<% end %>